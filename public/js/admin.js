function validate(){

    if(document.admin_form.username.value == '')
        {
             alert("plase fill the username");
        }
    if(document.admin_form.Password.value == '')
        {
            alert("plase fill the password");
        }
}
